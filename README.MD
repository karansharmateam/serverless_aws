--->sudo apt-get update
--->sudo apt instal npm
--->sudo npm install -g serverless
--->node -v (use latest version)
--->npm -v (use latest version)
# you can install nvm to reach latest version 
---> serverless
# you can choose and build any project from there we will choose here for this project
AWS-PYTHON-HTTTP API
# copy the link of serverleass link and paste into your browser to use serverless framework
# now you can use the server less framework
# now you have download the project temlate
# here you will get handler.py and serverless.yml
